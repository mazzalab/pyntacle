The ``extract`` command produces subgraphs from the input network, for example:

* selecting the **largest connected component**
* selecting the **n-th component**
* extracting the **largest N components** (or removing the last N)
* extracting components induced by a **given node list**
